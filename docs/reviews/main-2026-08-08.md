# Review: main (2026-08-08)

**审查对象**:smart_skills 仓库 `adversarial-development-loop` 标准化框架落地(未提交工作树;git diff HEAD + 未跟踪文件)。
**审查方式**:harness-driven Part B(B1 先选 harness,后读码);meta-bench 复跑获得字符串级证据。
**审查员**:ZCode(对上一轮实现的自审 + 独立对抗已在前一轮完成)。

---

## Harness: Harness-Driven Development Protocol (common/code-review/harness-driven-review.md)

- B1/B2 — PASS:先读 INDEX.md 选定 harness,后读代码。
- B3 — 本报告逐条应用,引用 file:line。
- B4 — Verdict Gate 见文末。
- B6 — **跳过并标注冲突**:`scripts/check_review_signals.py` 会写入 `common/meta/harness-feedback-log.md`(dev-guidelines 内),而用户级 AGENTS.md 明令该目录只读("勿修改其内容")。用户级指令优先于 (A) 级 harness 步骤,故不运行写入。若需反馈闭环,应先把该日志移出只读目录或由用户授权。

## Harness: Code Review Checklist (common/code-review/review-checklist.md)

- Item 1 Review Size — **FAIL (C)**:变更规模 ≈ 1100 行(tracked diff 398 插入/86 删除 + README 55 + untracked 562),远超 <400 行上限。修复路径:提交时按 logical unit 拆分(① 基线同步 154→397,② 框架文档 SKILL.md 速览/manual/README/openai.yaml,③ meta-bench 工具链 scripts/assets)。
- Item 2 Author Self-Check — PASS:实现后已按计划验证(meta-bench 7/7、demo 3 路、LF/JSON/py_compile/frontmatter/diff),结果入 LOOP-JOURNAL.md。
- Item 3 Correctness — PASS:复跑 `--verify` 7/7、`--demo` 三路 OK;7 份 manifest `json.tool` 通过;全部 Python `py_compile` 通过;SKILL.md 对 .zcode 副本 diff 仅 2 处计划内修改。边界覆盖:空 manifest/缺 fixture 由 `--verify`(run_meta_bench.py:158-186)拦截;例外见 error-handling FAIL。
- Item 4 Security — PASS:无注入面(`json.load` + 子串匹配,无 exec/eval/pickle/子进程);无密钥;无新依赖。
- Item 5 Test Coverage — PASS:`--demo` 内置三路自测(run_meta_bench.py:177-195)+ `--score` 三路样例报告验证;未覆盖路径(真实校准)已文档化为局限。
- Item 6 Readability — PASS,附 1 条 (A) 建议(见下 text-consistency)。
- Item 7 Performance — N/A(小规模 CLI,无热路径/网络)。
- Item 8 Review Speed — N/A(会话内完成)。

## Harness: Python Input Deserialization (python/security/input-deserialization.md, N)

- Item 1 Trust Boundary — PASS:manifest.json 视为 untrusted,`--verify` 做 schema/类型/枚举校验(run_meta_bench.py:81-104);报告文件仅做子串/正则匹配,从不执行。
- Item 2 Unsafe Formats — PASS:无 pickle/marshal;fixture.py 仅供人读与 `py_compile`(编译不执行),运行器从不导入/执行 fixture。
- Item 3 JSON/YAML — PASS:`json.load` + 必需字段/类型/枚举校验;未做"拒绝未知字段"(宽容)——(A) 可选收紧。
- Item 4 Resource Limits — PASS:N/A(toy 级、无网络、无大输入);错误不落盘。
- Item 5 AI/Tool Outputs — PASS:报告文件按 untrusted 处理;输出 schema 需求已在 framework-manual.md §3 文档化。

## Harness: AI Evaluation and Regression Strategy (common/ai/ai-evaluation-and-regression-strategy.md)

- Item 2 Dataset Composition — **FAIL (C)**:种子集 4 缺陷 + 3 干净全部 in-scope,缺 out-of-scope/refusal 类(本 skill 不应触发的输入)。**Justification**:meta-bench 定位为 toy 级冒烟测试(已在 framework-manual.md §3 与 README 诚实声明);建议补 1-2 个 out-of-scope 种子入 backlog。
- Item 3 Oracles — PASS:确定性结构化检查(JSON schema + 子串概念匹配 + 严重级)。
- Item 4 Regression Capture — PASS/N/A:无历史事故;结果入 journal。
- Item 5 Release Gates — N/A:无发布管道。
- Item 6 Change Management — **PARTIAL (A)**:改内核(缺口清单/评分规则)后应重跑 `--verify`+`--demo`,framework-manual.md 未显式要求 → 建议补一句。
- Item 7 Evidence — PASS:结果入 journal;但无结构化结果文件 → (A) 建议 `--score`/`--demo` 支持输出 JSON 便于聚合(对齐 youtu-agent 的 eval 记录思路)。

## Harness: Tool Calling and Agent Control (common/ai/tool-calling-and-agent-control.md)

- Item 1 Tool Contract — PASS:运行器只读(find/load/print),无副作用命令。
- Item 2 Least Privilege — PASS:无写/删/网络操作。
- Item 5 Loop Control — PASS:遍历种子有界;`--score` 单文件。
- Item 7 Auditability — PASS:结果打印 + journal 记录。

## Harness: Error Handling (common/error-handling/error-handling-strategy.md)

- Item 1 Choose strategy — PASS:CLI 约定非 0 退出 + 清晰消息(main → sys.exit)。
- Item 3 Never silently swallow — PASS:`--verify` 捕获 (OSError, ValueError) 并计数(run_meta_bench.py:165-170);`--score` 报告文件 OSError → sys.exit。
- **FAIL (C)**:`cmd_list`(run_meta_bench.py:191)与 `cmd_demo` 对损坏 manifest **无 try/except** → traceback 而非清晰报错。`cmd_verify`(165-170)已示范正确模式。修复:抽一个 `load_manifest_or_exit` 包装或复用 try 模式(3 行)。
- Item 6 Retry — N/A:无瞬态错误。

## Harness: Configuration Option Registration (common/config/option-registration-and-dimension.md)

- Item 1 Dimension — PASS:manifest 字段语义单一;flaw_type 枚举与内核缺口清单对齐。
- Item 2 注册位 — PASS:schema 唯一源 = framework-manual.md §2 + `validate_manifest` 同源实现。
- Item 4 磁盘名不可变 — PASS:id/flaw_type/expected_severity 冻结声明(manual §2);新增字段 additive。

## Harness: Prompt Injection and LLM Security (common/ai/prompt-injection-and-llm-security.md)

- Item 1 Instruction Boundary — PASS:报告/fixture 视为数据,评分器不将其当指令。
- Item 4 Output Handling — PASS:输出仅打印/评分,不进执行上下文。
- Item 7 Security Regression — PASS/N/A:无新增缓解措施。

## Harness: Testing (testing-strategy / test-doubles / differential-oracle-testing)

- testing-strategy — PASS:small 自测(`--demo`);无 pytest 套件是工具脚本定位的合理取舍。
- test-doubles — PASS:fixture 即 stub/golden 形态;诚实标注 manifest 人工标注。
- differential-oracle Item 4/5 — PASS:manifest 标注属 spec 类(验证意图);循环验证局限已文档化(manual §3,防"复制同一错误心智模型"已声明)。

## Harness: Documentation Standards (common/documentation/documentation-standards.md)

- PASS:README 一句话总结 + 快速上手(≤5 步命令)+ 链接到 framework-manual;注释 WHY(run_meta_bench.py:70-71 路径分隔符注释等)。
- N/A:CHANGELOG 未建(尚未提交;提交时补)。

## Harness: Naming Conventions (common/naming/naming-conventions.md)

- PASS:种子目录 kebab-case 意图命名(01-false-perfect…)、id 格式统一、脚本名意图清晰。

## Harness: Python Type Hints and mypy (python/correctness/type-hints-and-mypy.md)

- **FAIL (C) with justification**:run_meta_bench.py 函数无类型标注。**Justification**:270 行 CLI 工具脚本、非库代码、`--demo` 自测覆盖主要路径、stdlib-only 无 mypy 集成成本;若未来升级为可复用模块再补标注。归类为 approve-with-comments。

## 附加 (A) 级观察

1. **text-consistency (A)**:SKILL.md 速览小节用全角标点(，、)，基线正文用半角逗号(,)——风格不一致,微瑕。
2. **doc-tool wording (A)**:framework-manual.md §3 "全部 PASS = 本轮基准通过"未说明需逐种子 `--score` 或提供批量命令 → 建议加 `--score-all <报告目录>` 或改写措辞(stale-doc 类)。
3. **severity 标记契约 (A)**:评分器依赖 `[blocker]/[major]/[minor]` 括号标记(SEVERITY_PATTERN,run_meta_bench.py:114-119),手册未显式声明该契约 → 建议补一句。
4. **CI/自动回归 (A)**:meta-bench 无 CI 钩子(发行源无 CI);与 youtu-agent eval 的自动化差距,列入 backlog。

---

## 对照:TencentCloudADP/youtu-agent(仓库一手证据)

| 维度 | 本项目(落地) | youtu-agent |
|---|---|---|
| skill 实体 | **真实 SKILL.md + 7 种子 + 运行器,已验证 7/7** | skills 仅 `docs/howto/skills.md` 文档规范;**仓库 tree 内无任何真实 SKILL.md/.agent/skills 示例** |
| 目录/frontmatter | SKILL.md + references/scripts/assets/agents;name+description(+when_to_use) | `.agent/skills/<name>/{SKILL.md,references/}`;frontmatter 仅 name+description(子集,未文档化 scripts/assets) |
| 配置 schema | 无运行时框架;agents/openai.yaml 仅 UI manifest | 声明式 YAML:model / agent.instructions / env / context_manager / enabled_skills / toolkits / max_turns(`utu/config/agent_config.py` dataclass 证实) |
| 加载机制 | 渐进披露(metadata→SKILL.md→references 按需) | enabled_skills + `<available_skills>` 注入 + `openskills read`(需 shell_local env;**enabled_skills 消费代码仓库内未找到**) |
| 评估体系 | meta-bench:7 种子确定性评分 + --demo;结果终端/journal | `run_eval.py` + `configs/eval/*.yaml`(dataset/gt_field)+ 结果存数据库 + frontend/exp_analysis 看板;测 WebWalkerQA/GAIA/SWE-bench |
| 自动生成 | 无(手工加种子/领域包) | `gen_simple_agent.py` 元 agent 访谈→`configs/agents/generated/*.yaml`;`gen_tool.py`→MCP 工具(自动 venv,有产物证据 convert_video_to_gif) |
| 增强机制 | loop-journal 手动 in-context 记忆 + Pattern Index | Training-Free GRPO:模型冻结、~100 样本学 token prior 注入 instructions、~$8(`utu/practice/`) |

**对照结论**:

1. **我们有而他们缺**:落地成真的 skill 实体 + 自评测;他们的 skills 层停留在文档(无示例、enabled_skills 消费未证实)。
2. **他们强而我们缺**(按可移植性排序):
   - a. **声明式运行契约**(env/context_manager/enabled_skills):我们对抗者的"只读环境"只在 prompt 层表达,无配置层——初版计划拟加 agents/adversary.yaml 被过度工程砍掉,可作为未来增量(或轻量用 AgentSkills 的 `allowed-tools` 字段);
   - b. **评估结果持久化/聚合**(数据库+看板):我们的 meta-bench 结果不落盘;最小改进 = `--score` 输出 JSON + journal 结构化(已在做一半);
   - c. **自动生成 + 经验注入自动化**(meta-agent 访谈→配置;Training-Free GRPO 蒸馏经验注入 instructions):对应我们"领域包生成"与"journal 蒸馏自动化",目前纯手工。
3. **边界**:youtu-agent 是"框架 + 任意 skill 可插拔",我们是"单 skill 方法论 + 自评测";对照价值 = 按需移植其工程化机制,而非照抄规模。

---

## Verdict: REQUEST CHANGES

**门禁结果**:(N) 全 PASS;两处未 justify 的 (C) FAIL(规模拆分、error-handling traceback)+ 一处带 justification 的 (C)(eval 数据集组成)+ 一处 with-justification (C)(类型标注)。按 B4:存在未 justify 的 (C) → REQUEST CHANGES(轻微,修复量 ~10 行 + 提交拆分)。

**必改(REJECT 前)**:
1. (C) error-handling:run_meta_bench.py:191(cmd_list)与 cmd_demo 补 try/except(3 行)。
2. (C) 提交拆分:提交时按 3 个 logical unit(基线同步 / 框架文档 / meta-bench 工具链),满足 review-checklist Item 1。

**可选(带 justification 已接受)**:
3. (C) eval 数据集:补 out-of-scope 种子入 backlog(冒烟定位下可接受)。
4. (C) 类型标注:接受(工具脚本定位),未来模块化时补。

**建议(A)**:severity 括号契约补文档、`--score-all` 或措辞澄清、内核改动后重跑 meta-bench 一句、标点风格统一、`--score` JSON 输出、CI 钩子(backlog)。

---

## 修复确认(2026-08-08,第二轮)

| # | 项 | 处置 |
|---|----|------|
| 1 | (C) error-handling | **已修**:cmd_list/cmd_demo 补 try/except,损坏 manifest → 清晰报错 + exit 1,无 traceback(E2E 实测) |
| 2 | (C) 提交拆分 | 提交时执行(尚未提交) |
| 3 | (C) eval 数据集 | **已修**:新增 08-out-of-scope 种子,`--verify` 8/8 |
| 4 | (C) 类型标注 | 接受(工具脚本定位),未来模块化时补 |
| 5 | (A) severity 契约 | **已修**:framework-manual.md §3 显式声明 `[blocker]/[major]/[minor]` 括号契约 |
| 6 | (A) 基线措辞 | **已修**:改为"对每个种子分别 --score,全部 PASS 即基准通过" |
| 7 | (A) 内核重跑 | **已修**:manual §3 增加"内核改动后必须重跑 verify+demo+基准" |
| 8 | (A) `--score --json` | **已修**:结构化输出(seed/passed/reasons/flaw_type/expected_severity) |
| 9 | (A) 标点统一 | 不修:基线自身混用半角/全角,统一需动基线,违反零改动原则 |

## 更新后 Verdict: APPROVE

REQUEST CHANGES 的必改项已落地并通过验证;剩余为已接受的 justification 与 backlog(类型标注、CI 门禁、`--score-all`、真实样本回流、journal 校验)。
