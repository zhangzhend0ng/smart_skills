name: ci

on:
  push:
  pull_request:

jobs:
  meta-bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: meta-bench regression
        run: bash skills/adversarial-development-loop/scripts/ci_check.sh
